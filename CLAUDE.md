# Claude Code 项目入口

本文件仅用于兼容 Claude Code 的自动加载机制，不单独维护项目状态或实验数字。

开始任何工作前，完整读取 `AGENTS.md`，并严格按其第 0 节完成新会话初始化。该节已经规定根 README、当前文档总览、学习与审计主线、项目规则以及任务专项材料的读取顺序。

本文件不复制目录结构、项目状态、实验数字或业务规则，避免形成第二套入口口径。

测试集次数只读取 `docs/logs/test_set_runs.json`。历史归档中的数字和结论不得作为当前事实直接引用。
